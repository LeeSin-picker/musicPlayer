<view class="playList_container" bind:tap="goToMusicList">
	<image src="{{playList.coverImgUrl}}" class="playList_img"></image>
	<text class="playList_playCount">{{playList.commentCount}}</text>
	<view class="playList_name">{{playList.name}}</view>
</view>